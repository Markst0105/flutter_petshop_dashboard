name: petshop_dashboard
description: "Pet shop worker dashboard app built with Flutter."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: ^0.20.2
  fl_chart: ^1.2.0
  table_calendar: ^3.1.2
  provider: ^6.1.5
  animations: ^2.0.11
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^6.1.0
  mocktail: ^1.0.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
